# Tileable AI - 타일 텍스처 생성 도구

**상태**: 💡 idea  
**생성일**: 2026-01-07  
**분류**: apps  

---

## 개요

AI 기반으로 일반 이미지를 심리스(seamless) 타일 텍스처로 변환하는 독립 데스크톱 애플리케이션

---

## 왜 독립 도구인가?

| 측면 | 플러그인 형태 | 독립 단독 툴 |
|------|-----------|----------|
| **의존성** | 엔진 버전·플러그인 API 변경에 민감 | 자유로움 |
| **배포** | 엔진별로 따로 빌드/관리 필요 | 한 번만 빌드하면 여러 워크플로우에 적용 |
| **UI/UX** | 엔진 내 제약 있음 | 원하는 대로 자유 디자인 가능 |
| **배치 처리** | CLI 지원 어려움 | 배치/CLI 모드 구현 쉬움 |
| **커뮤니티** | 엔진 커뮤니티 내 국소적 | 별도 커뮤니티 구축 가능 |

---

## 아키텍처

```
TileableAI (Windows/macOS/Linux)
├─ 메인 UI (Python/PyQt6 + Qt Designer)
├─ 백엔드 처리 (Python + PyTorch)
├─ 타일 미리보기 (OpenGL/Vulkan)
└─ CLI / 배치 모드
```

### 주요 모듈

1. **입출력 모듈 (I/O)**
   - JPG, PNG, TGA, EXR, PSD 등 읽기
   - 4채널(RGBA), HDR 지원

2. **타일 합성 엔진**
   - **Graphcut** 기반 [기본, 빠름]
   - **Deep Tiling** 기반 [옵션, 느리지만 고품질]
   - **Generative AI** [옵션, 외부 API]

3. **품질 평가 모듈**
   - TexTile 개념 구현 (tileability score)
   - SSIM, FID 지표
   - 2D/3D 미리보기

4. **포스트 프로세싱**
   - Seam 보정 페인팅 도구
   - Normal/Height map 생성 (AI 기반)

5. **내보내기**
   - PBR 맵 세트 (Albedo, Normal, Roughness, Height, AO, Metallic)
   - 게임 엔진별 맞춤 네이밍

---

## 기술 스택

### 프론트엔드 (UI)
- **PyQt6** - 크로스플랫폼 GUI
- **OpenGL** - 3D 미리보기

### 백엔드 (처리)
```txt
PyQt6>=6.5.0
PyTorch>=2.0
opencv-python>=4.8
numpy>=1.24
scipy>=1.11
pymaxflow>=1.2
pillow>=10.0
tqdm>=4.66
pyyaml>=6.0
```

---

## 워크플로우

```
1. [이미지 로드]
   ↓
2. [전처리 옵션 설정]
   - 해상도 유지/변경
   - 색 공간 선택 (Linear/sRGB)
   ↓
3. [알고리즘 선택]
   ├─ Graphcut (빠름, 기본)
   ├─ Deep Tiling (느림, 고품질)
   └─ Generative AI (외부 API)
   ↓
4. [실시간 미리보기]
   - 타일 레이아웃 선택 (2×2, 3×3, 4×4)
   - Tileability score 표시
   ↓
5. [수동 보정] (선택)
   - 페인트 브러시로 seam 수정
   ↓
6. [내보내기]
   - 포맷/품질 선택
   - PBR 맵 생성 옵션
```

---

## CLI 사용 예시

```bash
# 기본 사용
python tileable_ai.py --input rock.jpg --output rock_tileable.png --method graphcut

# 배치 처리
python tileable_ai.py \
  --batch-dir ./raw_textures/ \
  --output-dir ./processed_tiles/ \
  --method graphcut \
  --generate-pbr
```

---

## 핵심 알고리즘

### Graphcut 타일링

```python
class GraphcutTiler:
    """그래프 컷 기반 타일 합성"""
    
    def __init__(self, image, tile_size=256, overlap=32):
        self.image = image
        self.tile_size = tile_size
        self.overlap = overlap
    
    def compute_seam_energy(self, patch1, patch2, direction):
        """두 패치의 겹치는 영역에서 seam 에너지 계산"""
        # ... 구현
    
    def find_seam_path(self, energy_map):
        """최저 에너지 경로 찾기 (Dynamic Programming)"""
        # ... 구현
    
    def create_tileable(self):
        """타일러블 텍스처 생성"""
        # ... 구현
```

### TexTile Score

```python
class SimpleTexTileScore(nn.Module):
    """간단한 tileability 점수 계산기"""
    
    def __init__(self):
        super().__init__()
        # VGG 백본 + 분류기
    
    def compute_tileability(self, image_np, tile_grid=(2, 2)):
        """NumPy 이미지에서 tileability score 계산"""
        # 0~1 스코어 반환
```

---

## 배포 전략

### 패키징 (PyInstaller)

```bash
pyinstaller --onefile --windowed --name TileableAI main.py
```

### 플랫폼별

- **Windows**: `TileableAI-1.0-setup.exe`
- **macOS**: `TileableAI-1.0.dmg`
- **Linux**: `tileable_ai-1.0-x86_64.AppImage`

---

## 예상 작업량

| 단계 | 기간 | 내용 |
|------|------|------|
| 1주차 | 1주 | UI/UX 설계, PyQt6 프레임워크 |
| 2주차 | 1주 | Graphcut 알고리즘 구현 |
| 3주차 | 1주 | 미리보기, 이미지 I/O |
| 4주차 | 1주 | Deep Tiling 옵션 |
| 5주차 | 1주 | TexTile 스코어 |
| 6주차 | 1주 | CLI, 배치 처리 |
| 7주차 | 1주 | 테스트, 최적화 |
| 8주차 | 1주 | 패키징, 배포 |

**총 예상**: 8주 (풀타임 기준)

---

## 타겟 사용자

1. **게임 개발자** - Unity, Unreal 텍스처 제작
2. **3D 아티스트** - 건축 렌더링, 제품 시각화
3. **디자이너** - 인쇄 패턴, 웹 배경
4. **인디 개발자** - 에셋 제작 효율화

---

## 수익화 가능성

| 모델 | 설명 |
|------|------|
| **무료 + 워터마크** | 기본 기능 무료, 워터마크 제거 유료 |
| **Freemium** | 기본 Graphcut 무료, Deep Tiling/AI 유료 |
| **일회성 구매** | $29~49 |
| **SaaS** | 클라우드 API 제공, 월 구독 |

---

## 참고 자료

- [TexTile 논문](https://arxiv.org/abs/...) - Tileability 품질 평가
- [Deep Image Tiling](https://...) - 딥러닝 기반 타일링
- [PyMaxflow](https://github.com/pmneila/PyMaxflow) - 그래프 컷 라이브러리

---

*마지막 업데이트: 2026-01-07*
